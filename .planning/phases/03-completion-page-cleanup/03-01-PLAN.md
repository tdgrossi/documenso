---
phase: 03-completion-page-cleanup
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/remix/app/routes/_recipient+/_layout.tsx
  - apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
autonomous: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
user_setup: []

must_haves:
  truths:
    - "Header is hidden on /sign/$token/complete route"
    - "Right-side sign-up panel is removed from completion page"
    - "Success content is centered"
    - "Download button and Go Back Home link remain for authenticated users"
    - "Sender's branding (CSS vars) still applied via RecipientBranding"
  artifacts:
    - path: apps/remix/app/routes/_recipient+/_layout.tsx
      provides: hideHeader logic updated to include complete route
      contains: sign.$token+/complete
    - path: apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
      provides: Simplified centered layout without sign-up panel
      min_lines: 280
  key_links:
    - from: _recipient+/_layout.tsx
      to: sign.$token+/complete.tsx
      via: hideHeader match
      pattern: "match?.id === 'routes/_recipient+/sign.$token+/complete'"
    - from: complete.tsx
      to: RecipientBranding
      via: component call preserved
      pattern: RecipientBranding
---

<objective>
Simplify the post-signing completion page by hiding the AuthenticatedHeader and removing the right-side sign-up panel, leaving a centered success message with download/home actions and sender's branding.
</objective>

<execution_context>
@C:/Users/max/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/max/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-completion-page-cleanup/03-CONTEXT.md
@.planning/REQUIREMENTS.md
apps/remix/app/routes/_recipient+/_layout.tsx
apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
apps/remix/app/components/general/recipient-branding.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide AuthenticatedHeader on complete route</name>
  <files>apps/remix/app/routes/_recipient+/_layout.tsx</files>
  <read_first>
    - apps/remix/app/routes/_recipient+/_layout.tsx
  </read_first>
  <action>
    In the `hideHeader` match condition at line 30-33, add a third condition to also match the complete route:

    Current (line 32):
    ```
    match?.id === 'routes/_recipient+/sign.$token+/_index' || match?.id === 'routes/_recipient+/d.$token+/_index'
    ```

    New (line 30-33):
    ```
    const hideHeader = matches.some(
      (match) =>
        match?.id === 'routes/_recipient+/sign.$token+/_index' ||
        match?.id === 'routes/_recipient+/d.$token+/_index' ||
        match?.id === 'routes/_recipient+/sign.$token+/complete',
    );
    ```

    Per D-02: Hide AuthenticatedHeader on `/sign/$token/complete` route via `hideHeader`.
  </action>
  <acceptance_criteria>
    - Line 32 now includes `|| match?.id === 'routes/_recipient+/sign.$token+/complete'` (or equivalent spread)
    - The hideHeader variable evaluates to true when the current route is `/sign/$token/complete`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --project apps/remix/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    AuthenticatedHeader is hidden on the /sign/$token/complete route per D-02 (COMP-02).
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove sign-up panel and center success content</name>
  <files>apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx</files>
  <read_first>
    - apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
  </read_first>
  <action>
    Make THREE changes to complete.tsx:

    1. SIMPLIFY OUTER DIV (lines 154-159):
    Remove the canSignUp conditional pt- classes. Replace:
    ```
    <div
      className={cn(
        '-mx-4 flex flex-col items-center overflow-hidden px-4 pt-16 md:-mx-8 md:px-8 lg:pt-20 xl:pt-28',
        { 'pt-0 lg:pt-0 xl:pt-0': canSignUp },
      )}
    ```
    With (always centered, no canSignUp dependency):
    ```
    <div className="-mx-4 flex flex-col items-center overflow-hidden px-4 pt-16 md:-mx-8 md:px-8 lg:pt-20 xl:pt-28">
    ```

    2. SIMPLIFY INNER DIV (lines 160-165):
    Remove the canSignUp conditional flex-row layout. Replace:
    ```
    <div
      className={cn('relative mt-6 flex w-full flex-col items-center justify-center', {
        'mt-0 flex-col divide-y overflow-hidden pt-6 md:pt-16 lg:flex-row lg:divide-x lg:divide-y-0 lg:pt-20 xl:pt-24':
          canSignUp,
      })}
    >
    ```
    With (always centered single column):
    ```
    <div className="relative mt-6 flex w-full max-w-2xl flex-col items-center justify-center">
    ```

    3. REMOVE SIGN-UP PANEL (lines 283-297):
    Delete the entire canSignUp conditional block:
    ```
          <div className="flex flex-col items-center">
            {canSignUp && (
              <div className="flex max-w-xl flex-col items-center justify-center p-4 md:p-12">
                <h2 className="mt-8 text-center font-semibold text-xl md:mt-0">
                  <Trans>Need to sign documents?</Trans>
                </h2>

                <p className="mt-4 max-w-[55ch] text-center text-muted-foreground/60 leading-normal">
                  <Trans>Create your account and start using state-of-the-art document signing.</Trans>
                </p>

                <ClaimAccount defaultName={recipientName} defaultEmail={recipient.email} />
              </div>
            )}
          </div>
    ```

    Per D-01: Remove entire right-side sign-up panel.
    Per D-04: Center success content by removing canSignUp conditional layout wrappers.
    Per D-05: Keep RecipientBranding component call (do not touch).
    Per D-06: Keep download button and "Go Back Home" link (do not touch lines 251-280).
    Per D-07: Leave canSignUp in loaderData (do not touch loader).
  </action>
  <acceptance_criteria>
    - Lines 283-297 (canSignUp panel) are deleted
    - Outer div no longer has canSignUp conditional pt- class
    - Inner div no longer has canSignUp conditional flex-row/divide-x classes, uses simple centered layout
    - RecipientBranding component call (line 153) is preserved
    - Download button and "Go Back Home" link (lines 251-280) are preserved
    - canSignUp remains in loaderData (line 94) but is not rendered
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --project apps/remix/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    Sign-up panel removed, success content centered per D-01/D-04 (COMP-03, COMP-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is UI-only cleanup with no security-sensitive changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Info Disclosure | complete.tsx | accept | No sensitive data exposed; removes panel with no data access changes |
| T-{phase}-SC | Tampering | npm/pip/cargo installs | not applicable | No new packages installed |
</threat_model>

<verification>
- `npx tsc --noEmit --project apps/remix/tsconfig.json` passes with no errors
- File structure: `_recipient+/_layout.tsx` has hideHeader check including `sign.$token+/complete`
- File structure: `complete.tsx` has no canSignUp conditional rendering, RecipientBranding preserved
</verification>

<success_criteria>
**Goal-backward verification:**

| Truth | How verified |
|-------|---------------|
| Header is hidden on /sign/$token/complete | hideHeader match includes `sign.$token+/complete` route ID |
| Right-side sign-up panel is removed | canSignUp conditional JSX block (lines 283-297) is deleted |
| Success content is centered | Layout uses simple flex-col centered div without canSignUp flex-row conditional |
| Download button and Go Back Home link remain | Lines 251-280 unchanged, still gated behind `user` check |
| Sender's branding still applied | RecipientBranding component call (line 153) preserved |
</success_criteria>

<output>
Create `.planning/phases/03-completion-page-cleanup/03-01-SUMMARY.md` when done
</output>
