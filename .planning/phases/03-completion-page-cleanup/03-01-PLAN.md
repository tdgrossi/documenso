---
phase: 03-completion-page-cleanup
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
autonomous: true
requirements:
  - COMP-03
  - COMP-04
  - COMP-05
user_setup: []

must_haves:
  truths:
    - "Right-side sign-up panel is removed from completion page"
    - "Success content is centered"
    - "Download button and Go Back Home link remain for authenticated users"
    - "Sender's branding (CSS vars) still applied via RecipientBranding"
  artifacts:
    - path: apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
      provides: Simplified centered layout without sign-up panel
      min_lines: 250
  key_links:
    - from: complete.tsx
      to: RecipientBranding
      via: component call preserved
      pattern: RecipientBranding
---

<objective>
Simplify the post-signing completion page by removing the right-side sign-up panel, leaving a centered success message with download/home actions and sender's branding.
</objective>

<execution_context>
@C:/Users/max/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/max/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-completion-page-cleanup/03-CONTEXT.md
@.planning/REQUIREMENTS.md
apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
apps/remix/app/components/general/recipient-branding.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove sign-up panel and center success content</name>
  <files>apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx</files>
  <read_first>
    - apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
  </read_first>
  <action>
    Make THREE changes to complete.tsx:

    1. SIMPLIFY OUTER DIV: Remove the canSignUp conditional pt- classes. Replace the cn()-based className with a simple fixed one (always centered, no canSignUp dependency):
    ```
    <div
      className="-mx-4 flex flex-col items-center overflow-hidden px-4 pt-16 md:-mx-8 md:px-8 lg:pt-20 xl:pt-28"
    >
    ```

    2. SIMPLIFY INNER DIV: Remove the canSignUp conditional flex-row layout. Replace with a simple centered single-column layout:
    ```
    <div className="relative mt-6 flex w-full max-w-2xl flex-col items-center justify-center">
    ```

    3. REMOVE SIGN-UP PANEL: Delete the entire canSignUp conditional block (the right-side panel with "Need to sign documents?" heading and ClaimAccount component).

    Per D-01: Remove entire right-side sign-up panel.
    Per D-04: Center success content by removing canSignUp conditional layout wrappers.
    Per D-05: Keep RecipientBranding component call (do not touch).
    Per D-06: Keep download button and "Go Back Home" link.
    Per D-07: Leave canSignUp in loaderData but do not render it.
  </action>
  <acceptance_criteria>
    - canSignUp panel JSX block is deleted (right-side sign-up CTA gone)
    - Outer div uses simple fixed className, no canSignUp conditional pt- classes
    - Inner div uses simple centered layout, no canSignUp conditional flex-row/divide-x
    - RecipientBranding component call is preserved
    - Download button and "Go Back Home" link are preserved
    - canSignUp remains in loaderData but is not rendered in UI
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --project apps/remix/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    Sign-up panel removed, success content centered (COMP-03, COMP-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is UI-only cleanup with no security-sensitive changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | This phase is UI-only cleanup | no new threats | — | — |
</threat_model>

<verification>
- `npx tsc --noEmit --project apps/remix/tsconfig.json` passes with no errors
- File structure: `complete.tsx` has no canSignUp conditional rendering, RecipientBranding preserved
</verification>

<success_criteria>
**Goal-backward verification:**

| Truth | How verified |
|-------|---------------|
| Right-side sign-up panel is removed | canSignUp conditional JSX block is deleted |
| Success content is centered | Layout uses simple flex-col centered div without canSignUp flex-row conditional |
| Download button and Go Back Home link remain | Still gated behind `user` check, not modified |
| Sender's branding still applied | RecipientBranding component call preserved |
</success_criteria>

<output>
Create `.planning/phases/03-completion-page-cleanup/03-01-SUMMARY.md` when done
</output>
