---
phase: 03-completion-page-cleanup
plan: '02'
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
autonomous: true
requirements:
  - COMP-01
user_setup: []

must_haves:
  truths:
    - "Completion page displays the sender's logo (from team/org branding settings) instead of Documenso logo"
    - "Logo is shown near the success message heading"
  artifacts:
    - path: apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
      provides: Sender logo display in success area
      contains: brandingLogoUrl
  key_links:
    - from: complete.tsx loader
      to: getTeamSettings
      via: document.teamId used to load brandingLogo
      pattern: "getTeamSettings"
---

<objective>
Display the sender's user/org logo (from branding settings) on the completion page, replacing Documenso branding with the sender's identity.
</objective>

<execution_context>
@C:/Users/max/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/max/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-completion-page-cleanup/03-CONTEXT.md
@.planning/REQUIREMENTS.md
apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
packages/lib/server-only/team/get-team-settings.ts
apps/remix/app/components/general/recipient-branding.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sender logo URL to loader data</name>
  <files>apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx</files>
  <read_first>
    - apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
    - packages/lib/server-only/team/get-team-settings.ts
  </read_first>
  <action>
    Modify the loader in complete.tsx to also return the sender's branding logo URL.

    1. ADD IMPORT at the top of the file (near line 5):
    Import `getTeamSettings` from `@documenso/lib/server-only/team/get-team-settings`

    2. MODIFY LOADER to fetch and return brandingLogoUrl (after line 52):
    After `const branding = await loadRecipientBrandingByTeamId({ teamId: document.teamId });`, add:

    ```typescript
    const teamSettings = await getTeamSettings({ teamId: document.teamId });
    const brandingLogoUrl = teamSettings.brandingLogo
      ? `/api/branding/logo/team/${document.teamId}`
      : null;
    ```

    3. ADD to the return object (around line 92-102):
    Add `brandingLogoUrl` to the return statement so it's available in the component:

    ```typescript
    return {
      // ... existing fields ...
      brandingLogoUrl,  // add this
      branding,         // keep existing
    };
    ```

    Note: Add `brandingLogoUrl` to the return in BOTH return statements in the loader (lines 71-75 for unauthorized access and lines 92-102 for successful access).

    Per COMP-01: Sender's logo comes from team/org branding settings via getTeamSettings.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/remix/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    Loader returns brandingLogoUrl for use in the component.
  </done>
</task>

<task type="auto">
  <name>Task 2: Display sender logo in completion page success area</name>
  <files>apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx</files>
  <read_first>
    - apps/remix/app/routes/_recipient+/sign.$token+/complete.tsx
  </read_first>
  <action>
    After implementing Task 1, update the component to display the sender's logo.

    1. ADD to destructured loaderData (around line 112-122):
    Add `brandingLogoUrl` to the destructured loaderData:

    ```typescript
    const {
      // ... existing fields ...
      brandingLogoUrl,  // add this
    } = loaderData;
    ```

    2. ADD logo display above the success heading (around line 184, before the h2):
    Insert this block before the `<h2>` element at line 184:

    ```typescript
            {brandingLogoUrl && (
              <img
                src={brandingLogoUrl}
                alt="Sender logo"
                className="mb-4 h-12 w-auto object-contain"
              />
            )}
    ```

    The logo appears between the SigningCard3D (line 182) and the "Document Signed/Viewed/Approved" heading (line 184).

    Per COMP-01: Display sender's user/org logo instead of Documenso branding.
    Note: If no logo is set (brandingLogoUrl is null), nothing renders — this is the correct fallback behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/remix/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    Sender's logo is displayed on the completion page when available in branding settings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase is UI-only change with no security-sensitive changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Info Disclosure | complete.tsx | accept | Logo URL reveals only team ID; no sensitive data |
| T-{phase}-SC | Tampering | npm/pip/cargo installs | not applicable | No new packages |
</threat_model>

<verification>
- `npx tsc --noEmit --project apps/remix/tsconfig.json` passes with no errors
- `brandingLogoUrl` is destructured from loaderData in the component
- `brandingLogoUrl` is fetched via `/api/branding/logo/team/{teamId}` when `teamSettings.brandingLogo` is set
- Logo `<img>` element renders conditionally when `brandingLogoUrl` is truthy
</verification>

<success_criteria>
**Goal-backward verification:**

| Truth | How verified |
|-------|---------------|
| Completion page displays sender's logo | `brandingLogoUrl` populated from `getTeamSettings().brandingLogo` and rendered as `<img>` |
| Logo shown near success message heading | `<img>` inserted before the `<h2>` at line 184 |
</success_criteria>

<output>
Create `.planning/phases/03-completion-page-cleanup/03-02-SUMMARY.md` when done
</output>
